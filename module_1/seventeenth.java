package submissions.module_1;

public class seventeenth {
    public static void main(String[] args) {
        // Valid identifiers
        int myVar = 10;
        int _value = 20;
        int $count = 30;
        int number1 = 40;

        // Invalid identifiers (will cause compile-time errors if uncommented)
        // int 1number = 50; // cannot start with a digit
        // int class = 60;   // 'class' is a reserved keyword
        // int my-var = 70;  // hyphens not allowed

        System.out.println("Valid identifiers: myVar, _value, $count, number1");
    }
}
